import { globals, colors, fonts } from '../../config/constants';

export const navigationStyles = {
	header: {
		fontFamily: fonts.OPENSANS_SEMI_BOLD,
		color: colors.HEADER_TITLE
	},
	tabBar: {
		height: globals.NAVBAR_HEIGHT
	}
};
